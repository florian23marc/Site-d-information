{% extends 'base.html.twig' %}

{% block title %} Détails de l'article {% endblock %}  

{% block body %}

<div class="container mt-5 p-lg-5">
    <div class="row">
        <div class="col-lg-12 text-center">
            <h1 class="mb-4">{{ showArticlesMiddle.title }}</h1>
            <p class="lead mb-4">{{ showArticlesMiddle.subtitle }}</p>
            <small class="text-muted">{{ showArticlesMiddle.publishedAt | date('d-m-Y') }}</small>
        </div>
    </div>
    <div class="row justify-content-center">
        <div class="col-lg-10">
            <img class="card-img-top img-fluid rounded shadow-lg mb-5" src="{{ asset('uploads/' ~ showArticlesMiddle.image) }}" alt="{{ showArticlesMiddle.image }}">
        </div>
    </div>
    <div class="row">
        <div class="col-lg-12">
            <p>{{ showArticlesMiddle.description | raw }}</p>
        </div>
    </div>
</div>
{% endblock %}